# ==============================================
# Malifax Docker Configuration
# ==============================================
# Copy this file to .env and customize for your environment

# -----------------
# Port Configuration
# -----------------
# Frontend port (exposed to host)
# Default: 3080 (to avoid conflicts with other apps using 3000)
FRONTEND_PORT=3080

# Backend API port (exposed to host)
# Default: 4200 (to avoid conflicts with other apps using 4000)
BACKEND_PORT=4200

# MongoDB port (exposed to host)
# Default: 27018 (to avoid conflicts with existing MongoDB on 27017)
MONGO_PORT=27018

# -----------------
# MongoDB Configuration
# -----------------
MONGO_USER=admin
MONGO_PASSWORD=admin123
MONGO_DB=malifax

# -----------------
# Backend API Configuration
# -----------------
# JWT Secret for authentication (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=malifax-admin-secret-key-2026

# -----------------
# Frontend Configuration
# -----------------
# API URL that frontend will call
# For local: http://localhost:4200 (matches BACKEND_PORT)
# For production: https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:4200

# ==============================================
# Production Example (uncomment and modify):
# ==============================================
# FRONTEND_PORT=8080
# BACKEND_PORT=8081
# MONGO_PORT=27018
# MONGO_USER=malifax_admin
# MONGO_PASSWORD=your-secure-password-here
# MONGO_DB=malifax_production
# JWT_SECRET=your-very-long-random-secret-key-here
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
