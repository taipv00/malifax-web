# Backend Environment Variables
# Copy this file to .env and update with your actual values

# MongoDB Connection String
MONGODB_URI=mongodb://admin:admin123@mongodb:27017/malifax?authSource=admin

# Server Port
PORT=4000

# JWT Secret (REQUIRED - must match docker-compose.yml)
JWT_SECRET=malifax-admin-secret-key-2026

# Default Admin Password (optional, for first-time setup)
DEFAULT_ADMIN_PASSWORD=change-this-password
